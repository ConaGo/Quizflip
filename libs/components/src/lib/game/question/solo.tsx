import { useQuery } from '@apollo/client';
//import { GET_RANDOM_QUESTIONS } from '@libs/data-access';
export const SoloQuestionGame = () => {
  /*   const { loading, error, data } = useQuery(GET_RANDOM_QEUSTIONS, {
    variables: { count: 10 },
  }); */
  return <p>SoloQuestionGame</p>;
};
